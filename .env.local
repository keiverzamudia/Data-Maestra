# Pruebas — perfil de puertos de ESTE worktree (lo leen scripts/_worktree.ps1).
# API_PORT/WEB_PORT del secundario. No duplicar el resto aquí:
# la config vive en .env raíz y los secretos en apps/api/.env.local.
API_PORT=3002
WEB_PORT=5174
